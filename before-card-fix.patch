diff --git a/script.js b/script.js
index 9b28ebd..a12352f 100644
--- a/script.js
+++ b/script.js
@@ -113,13 +113,13 @@ const updateParallax = () => {
 
   parallaxHeroes.forEach((hero) => {
     const rect = hero.getBoundingClientRect();
-    const offset = clamp(-70, rect.top * -0.2, 96);
+    const offset = clamp(-52, rect.top * -0.14, 72);
 
     hero.style.setProperty("--hero-parallax-y", `${offset}px`);
   });
 
   if (pageShell) {
-    const bodyOffset = clamp(0, window.scrollY * 0.065, 118);
+    const bodyOffset = clamp(0, window.scrollY * 0.045, 86);
 
     pageShell.style.setProperty("--body-parallax-y", `${bodyOffset}px`);
   }
@@ -134,12 +134,11 @@ const requestParallax = () => {
   }
 };
 
-updateParallax();
+resetParallax();
 
-window.addEventListener("scroll", requestParallax, { passive: true });
 window.addEventListener("resize", () => {
   syncPageBackgroundStart();
-  requestParallax();
+  resetParallax();
 });
 
 if (typeof prefersReducedMotion.addEventListener === "function") {
diff --git a/styles.css b/styles.css
index 076236f..7292427 100644
--- a/styles.css
+++ b/styles.css
@@ -2168,36 +2168,81 @@ body::before {
 .home-hero + .section .approach-card {
   min-height: 100%;
   padding: 34px 30px 32px;
-  border-color: rgba(245, 239, 227, 0.14);
+  isolation: isolate;
+  backface-visibility: hidden;
+  background-clip: padding-box;
+  border-color: rgba(91, 68, 44, 0.28);
   background:
-    linear-gradient(135deg, rgba(14, 18, 13, 0.42), rgba(15, 22, 17, 0.24)),
-    rgba(12, 17, 12, 0.18);
+    radial-gradient(circle at 14% 16%, rgba(255, 244, 219, 0.2), transparent 9rem),
+    radial-gradient(circle at 88% 84%, rgba(112, 86, 54, 0.16), transparent 10rem),
+    repeating-linear-gradient(
+      108deg,
+      rgba(92, 70, 45, 0.035) 0 1px,
+      transparent 1px 18px
+    ),
+    linear-gradient(145deg, #b49b72 0%, #92754f 46%, #695138 100%);
   box-shadow:
-    0 24px 64px rgba(0, 0, 0, 0.2),
-    inset 0 1px 0 rgba(255, 255, 255, 0.06);
-  backdrop-filter: blur(10px);
+    0 24px 64px rgba(0, 0, 0, 0.24),
+    inset 0 1px 0 rgba(255, 248, 226, 0.3),
+    inset 0 -28px 48px rgba(53, 39, 25, 0.18);
+  backdrop-filter: none;
 }
 
 .home-hero + .section .approach-card::before {
+  content: "";
+  display: none;
+}
+
+.home-hero + .section .approach-card::after {
+  content: "";
+  position: absolute;
+  left: 16px;
+  right: 16px;
+  top: 0;
+  z-index: 0;
+  height: 2px;
+  pointer-events: none;
+  border-radius: 999px;
+  background: linear-gradient(90deg, transparent, rgba(255, 244, 219, 0.72), transparent);
+  box-shadow: 0 0 12px rgba(255, 230, 172, 0.16);
   opacity: 0.68;
-  background:
-    radial-gradient(circle at 18% 12%, rgba(255, 250, 226, 0.3), transparent 9rem),
-    radial-gradient(circle at 88% 92%, rgba(67, 86, 52, 0.1), transparent 10rem);
+  transition:
+    opacity 220ms ease,
+    box-shadow 220ms ease,
+    left 220ms ease,
+    right 220ms ease,
+    background-color 220ms ease;
+}
+
+.home-hero + .section .approach-card:hover::after {
+  left: 10px;
+  right: 10px;
+  opacity: 1;
+  background: linear-gradient(90deg, transparent, rgba(236, 221, 176, 0.98), transparent);
+  box-shadow: 0 0 18px rgba(192, 170, 120, 0.24);
+}
+
+.home-hero + .section .approach-card > * {
+  position: relative;
+  z-index: 1;
 }
 
 .home-hero + .section .approach-card .card-label {
-  color: var(--lichen-soft);
+  color: #4b3927;
+  text-shadow: 0 1px 0 rgba(255, 248, 226, 0.28);
 }
 
 .home-hero + .section .approach-card h3 {
   margin-bottom: 12px;
-  color: #fff4df;
-  text-shadow: 0 3px 22px rgba(0, 0, 0, 0.52);
+  color: #fff6df;
+  text-shadow:
+    0 2px 12px rgba(53, 39, 25, 0.42),
+    0 1px 0 rgba(74, 51, 30, 0.42);
 }
 
 .home-hero + .section .approach-card p {
-  color: rgba(245, 239, 227, 0.82);
-  text-shadow: 0 2px 18px rgba(0, 0, 0, 0.46);
+  color: rgba(255, 250, 238, 0.9);
+  text-shadow: 0 2px 12px rgba(53, 39, 25, 0.38);
 }
 
 @keyframes soft-rise {
@@ -2227,11 +2272,31 @@ body::before {
 
 @media (hover: hover) and (pointer: fine) {
   .home-hero + .section .approach-card:hover {
-    transform: translateY(-3px);
-    border-color: rgba(245, 239, 227, 0.22);
+    transform: translateY(-10px) scale(1.02) translateZ(0);
+    border-color: rgba(255, 239, 196, 0.58);
+    background:
+      radial-gradient(circle at 16% 12%, rgba(255, 248, 226, 0.28), transparent 9rem),
+      radial-gradient(circle at 88% 88%, rgba(105, 75, 44, 0.18), transparent 10rem),
+      repeating-linear-gradient(
+        108deg,
+        rgba(92, 70, 45, 0.04) 0 1px,
+        transparent 1px 18px
+      ),
+      linear-gradient(145deg, #c4aa7d 0%, #9f8058 46%, #70553a 100%);
     box-shadow:
-      0 28px 68px rgba(0, 0, 0, 0.24),
-      inset 0 1px 0 rgba(255, 255, 255, 0.07);
+      0 42px 92px rgba(0, 0, 0, 0.42),
+      0 0 0 1px rgba(255, 239, 196, 0.2),
+      0 0 28px rgba(196, 168, 111, 0.16),
+      inset 0 1px 0 rgba(255, 248, 226, 0.44),
+      inset 0 -30px 52px rgba(53, 39, 25, 0.18);
+  }
+
+  .home-hero + .section .approach-card:hover::after {
+    left: 10px;
+    right: 10px;
+    opacity: 1;
+    background: linear-gradient(90deg, transparent, rgba(255, 248, 226, 0.96), transparent);
+    box-shadow: 0 0 22px rgba(255, 230, 172, 0.28);
   }
 }
 
@@ -2453,6 +2518,143 @@ main::before,
   background-repeat: no-repeat;
 }
 
+/* Refined interactive polish */
+
+.site-nav a:not(.nav-cta) {
+  position: relative;
+  padding-bottom: 7px;
+}
+
+.site-nav a:not(.nav-cta)::after {
+  content: "";
+  position: absolute;
+  left: 50%;
+  right: 50%;
+  bottom: 0;
+  height: 2px;
+  border-radius: 999px;
+  background:
+    radial-gradient(circle at 50% 50%, rgba(255, 239, 183, 0.95), transparent 68%),
+    linear-gradient(90deg, transparent, rgba(196, 168, 111, 0.95), rgba(245, 239, 227, 0.7), rgba(196, 168, 111, 0.95), transparent);
+  opacity: 0;
+  box-shadow: 0 0 12px rgba(196, 168, 111, 0.28);
+  transition:
+    left 220ms ease,
+    right 220ms ease,
+    opacity 220ms ease;
+}
+
+.site-nav a:not(.nav-cta):hover::after,
+.site-nav a:not(.nav-cta):focus-visible::after,
+.site-nav a:not(.nav-cta)[aria-current="page"]::after {
+  left: 3px;
+  right: 3px;
+  opacity: 1;
+}
+
+.nav-cta {
+  box-shadow:
+    inset 0 1px 0 rgba(255, 248, 226, 0.12),
+    0 0 0 rgba(196, 168, 111, 0);
+  transition:
+    transform 200ms ease,
+    border-color 200ms ease,
+    background-color 200ms ease,
+    box-shadow 200ms ease;
+}
+
+.button:focus-visible,
+.nav-cta:focus-visible,
+.footer-links a:focus-visible,
+.brand:focus-visible {
+  outline: 2px solid rgba(196, 168, 111, 0.7);
+  outline-offset: 4px;
+}
+
+@media (hover: hover) and (pointer: fine) {
+  .nav-cta:hover {
+    transform: translateY(-1px);
+    background-color: rgba(245, 239, 227, 0.06);
+    border-color: rgba(196, 168, 111, 0.9);
+    box-shadow:
+      inset 0 1px 0 rgba(255, 248, 226, 0.18),
+      0 10px 24px rgba(0, 0, 0, 0.18);
+  }
+
+  .button:hover {
+    transform: translateY(-3px);
+    filter: saturate(1.08);
+    border-color: rgba(196, 168, 111, 0.76);
+    box-shadow:
+      inset 0 1px 0 rgba(255, 255, 255, 0.42),
+      0 18px 38px rgba(0, 0, 0, 0.24),
+      0 0 28px rgba(196, 168, 111, 0.12);
+  }
+
+  main > .section:not(.footer-scene) .card:not(.approach-card):hover,
+  .footer-scene .card:hover,
+  .contact-form-card:hover,
+  .contact-next-card:hover,
+  .featured-build-panel:hover,
+  .projects-cta-panel:hover {
+    border-color: rgba(196, 168, 111, 0.42);
+    box-shadow:
+      0 34px 84px rgba(0, 0, 0, 0.3),
+      inset 0 1px 0 rgba(255, 255, 255, 0.12),
+      inset 0 0 42px rgba(196, 168, 111, 0.055),
+      0 0 28px rgba(196, 168, 111, 0.07);
+  }
+
+  main > .section:not(.footer-scene) .card:not(.approach-card):hover::before,
+  .footer-scene .card:hover::before {
+    background:
+      radial-gradient(circle at -8% -12%, rgba(255, 224, 154, 0.24), transparent 7.5rem),
+      radial-gradient(circle at 108% 112%, rgba(83, 103, 69, 0.1), transparent 9rem);
+    opacity: 0.68;
+  }
+}
+
+.field-group input,
+.field-group textarea,
+.field-group select {
+  transition:
+    border-color 180ms ease,
+    box-shadow 180ms ease,
+    background-color 180ms ease;
+}
+
+.field-group input:focus,
+.field-group textarea:focus,
+.field-group select:focus {
+  border-color: rgba(196, 168, 111, 0.92);
+  background-color: rgba(255, 250, 238, 0.97);
+  box-shadow:
+    0 0 0 4px rgba(196, 168, 111, 0.18),
+    0 10px 24px rgba(0, 0, 0, 0.12),
+    inset 0 1px 0 rgba(255, 255, 255, 0.5);
+}
+
+@media (max-width: 920px) {
+  .site-nav {
+    background:
+      radial-gradient(circle at 16% 10%, rgba(196, 168, 111, 0.11), transparent 14rem),
+      linear-gradient(145deg, rgba(24, 21, 16, 0.98), rgba(12, 18, 12, 0.96));
+    border-color: rgba(196, 168, 111, 0.24);
+    box-shadow:
+      0 24px 54px rgba(0, 0, 0, 0.38),
+      inset 0 1px 0 rgba(255, 248, 226, 0.08);
+  }
+
+  .site-nav a:not(.nav-cta) {
+    padding-bottom: 9px;
+    border-bottom: 1px solid rgba(245, 239, 227, 0.08);
+  }
+
+  .site-nav a:not(.nav-cta)::after {
+    display: none;
+  }
+}
+
 @media (prefers-reduced-motion: reduce) {
   html {
     scroll-behavior: auto;
